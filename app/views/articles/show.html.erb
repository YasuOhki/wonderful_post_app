<p id="notice"><%= notice %></p>

<div class="card">
  <div class="card-header">
    <%= @article.title %>
  </div>
  <div class="card-body">
    <%= @article.content %>
  </div>
</div>

<%= link_to t('.edit'), edit_article_path(@article), class: "btn btn-warning" %>
<%= link_to t('.back'), articles_path, class: "btn btn-secondary" %>
